{
  "name": "kg",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "test": "depcruise src quartz-plugins --config .dependency-cruiser.cjs && tsc --noEmit && node --test 'spec/*.spec.ts'",
    "test:db": "node --test --test-concurrency=1 'spec/db/*.spec.ts'"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "@types/pg": "^8.23.1",
    "dependency-cruiser": "^18.4.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
